spring:
  datasource:
    url: jdbc:h2:mem:posts_db
    username: admin
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
